name: Build, Deploy and Release

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

permissions:
  contents: write

env:
  CSPROJ_PATH: ./src/ExpForge.Documentation.Presentation/ExpForge.Documentation.Presentation.csproj

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      publish-path: ${{ steps.publish.outputs.publish_dir }}
      new-version: ${{ steps.version-check.outputs.new_version }}
      current-version: ${{ steps.version-check.outputs.current_version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # necessário para pegar tags

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - run: dotnet restore

      - id: publish
        run: |
          dotnet publish -c Release -o release
          echo "publish_dir=release/wwwroot" >> $GITHUB_OUTPUT

      - run: sed -i 's|<base href="/" />|<base href="/ExpForge.Documentation/" />|' release/wwwroot/index.html

      - name: Check version difference
        id: version-check
        run: |
          set -e
          CURRENT_VERSION=$(grep -oPm1 "(?<=<Version>)[^<]+" "${{ env.CSPROJ_PATH }}")
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $CURRENT_VERSION"

          LAST_TAG=$(git tag --list "v*" | sort -V | tail -n1)
          if [ -z "$LAST_TAG" ]; then
            LAST_VERSION="0.0.0"
          else
            LAST_VERSION="${LAST_TAG#v}"
          fi
          echo "Last published version: $LAST_VERSION"

          if [ "$CURRENT_VERSION" != "$LAST_VERSION" ]; then
            echo "✅ Nova versão detectada: $CURRENT_VERSION (última: $LAST_VERSION)"
            echo "new_version=true" >> $GITHUB_OUTPUT
          else
            echo "ℹ️ Nenhuma nova versão detectada ($CURRENT_VERSION)."
            echo "new_version=false" >> $GITHUB_OUTPUT
          fi

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' && needs.build.outputs.new-version == 'true' }}
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ${{ needs.build.outputs.publish-path }}

  release:
    runs-on: ubuntu-latest
    needs: deploy
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/master' && needs.build.outputs.new-version == 'true' }}
    steps:
      - uses: actions/checkout@v4

      - name: Create GitHub Release
        uses: actions/create-release@v1
        with:
          tag_name: v${{ needs.build.outputs.current-version }}
          release_name: Release v${{ needs.build.outputs.current-version }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
